/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'plus.unsplash.com', 'tailwindui.com', 'static.nike.com'],
    },
};

export default nextConfig;
